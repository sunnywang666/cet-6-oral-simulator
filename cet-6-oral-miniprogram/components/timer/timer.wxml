<!--components/timer/timer.wxml-->
<view class="timer-container">
  <view class="timer-circle {{variant}}">
    <view class="timer-content">
      <text class="timer-icon">⏱</text>
      <text class="timer-text">{{formattedTime}}</text>
    </view>
  </view>
  <text class="timer-label" wx:if="{{label}}">{{label}}</text>
  <view class="progress-bar">
    <view class="progress-fill {{variant}}" style="width: {{progress}}%"></view>
  </view>
</view>



